sudo: false

language:
  - cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - libboost-all-dev

script:
  - make
  - ~/bin/demo
  - ~/bin/test.run

notifications:
  email: false
